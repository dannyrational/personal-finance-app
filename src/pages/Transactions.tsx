export default function Transactions() {
  return (
    <div className="page-transactions">
      <h1>Transactions</h1>
      <p>Search, sort, and filter transactions with pagination (10 per page).</p>
    </div>
  )
}
